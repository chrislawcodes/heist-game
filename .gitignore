__pycache__/
*.py[cod]
.venv/
venv/
.env
.DS_Store
.claude/
.mypy_cache/
.pytest_cache/
.ruff_cache/
/logs/
# Gemini --yolo occasionally writes its JSON response to disk as a side effect.
# We don't want these in the repo. Real outputs go to examples/ via --out.
/casting_summary.json
/bid.json
/narration.json
